IMPORT $;
IMPORT $.DeclareData AS D;

SEQUENTIAL($.File_Bible.Bld,$.Inversion.Bld);

// MaxLen := RECORD
   // Ml := MAX(GROUP,LENGTH(TRIM($.File_Bible.Bible.Text)));
// END;

// OUTPUT(TABLE($.File_Bible.Bible,MaxLen));


